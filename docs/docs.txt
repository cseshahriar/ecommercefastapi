python -m venv venv

source venv\bin\activate
source venv\scripts\activate

pip install "fastapi[standard]" sqlalchemy[asyncio] aiomysql alembic python-decouple python-jose[cryptography] passlib[bcrypt] cryptography

pip freeze > requirements.txt

fastapi dev app/main.py or
uvicorn app.main:app --reload --host localhost --port 8000

alembic init -t async alembic
alembic revision --autogenerate -m "create table"
alembic upgrade head
alembic stamp head

alembic revision --autogenerate -m "Create user and refresh token tables"
alembic upgrade head

alembic revision --autogenerate -m "Create product and category tables"

# after delete alembic version
alembic stamp head

ALTER TABLE products AUTO_INCREMENT = 1;

.env variables
DB_USER=
DB_PASS=
DB_NAME=
DB_PORT=3306
DB_HOST=localhost

JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_TIME_MIN=5
JWT_REFRESH_TOKEN_TIME_DAY=7
EMAIL_VERIFICATION_TOKEN_TIME_HOUR=1
EMAIL_PASSWORD_RESET_TOKEN_TIME_HOUR=2
FRONTEND_URL=http://localhost:3000
DEBUG=True